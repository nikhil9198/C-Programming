#include<stdio.h>
int main() 
{
    int a;  //variable declaration
    scanf("%d",&a); //it is used to user input 
    printf("The value of a is  %d",a);  //its prints the value of a given by user
    return 0;
}